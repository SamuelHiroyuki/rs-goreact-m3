const INITIAL_STATE = [{ id: 1, text: 'Hello World' }, { id: 2, text: 'Dlrow Olleh' }];

export default function todos(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
